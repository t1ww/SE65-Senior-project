version: '3.8'

services:
  ts-code-runner:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ts-code-runner
    ports:
      - "3000:3000"
    volumes:
      - .:/app
    command: npx ts-node server.ts  # Ensure the correct start command
