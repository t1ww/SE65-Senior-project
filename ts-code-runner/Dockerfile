# Use a small Node.js image
FROM node:18-alpine

# Install TypeScript and ts-node globally
RUN npm install -g typescript ts-node

# Set the working directory
WORKDIR /app

# Default command (does nothing, waits for input)
CMD ["sh"]
